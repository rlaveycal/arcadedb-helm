suite: Observability args (asserted via StatefulSet)
templates:
  - statefulset.yaml
release:
  name: test
  namespace: default
tests:
  - it: emits no observability args by default
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.logFormat=json"
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.serverMetrics.otlp.enabled=true"
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.serverMetrics.tracing.enabled=true"
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.readinessRequiresHA=true"
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.readinessHAMaxLag=100"
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.logIncludeTrace=true"
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.serverMetrics.otlp.endpoint=http://localhost:4317"
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.serverMetrics.tracing.samplingRate=0.0"
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "Otlp:com.arcadedb.metrics.otlp.OtlpMetricsPlugin"
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "Tracing:com.arcadedb.tracing.TracingPlugin"

  - it: logging.format=json emits the logFormat arg
    set:
      observability.logging.format: json
    asserts:
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.logFormat=json"

  - it: text logging (default) emits no logFormat arg
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.logFormat=json"

  - it: logging.includeTrace=true emits the logIncludeTrace arg
    set:
      observability.logging.includeTrace: true
    asserts:
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.logIncludeTrace=true"

  - it: otlp metrics enabled emits enable + endpoint args
    set:
      observability.metrics.otlp.enabled: true
      observability.metrics.otlp.endpoint: http://otel-collector:4317
    asserts:
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.serverMetrics.otlp.enabled=true"
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.serverMetrics.otlp.endpoint=http://otel-collector:4317"

  - it: otlp metrics enabled registers the OtlpMetricsPlugin
    set:
      observability.metrics.otlp.enabled: true
    asserts:
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.plugins=Otlp:com.arcadedb.metrics.otlp.OtlpMetricsPlugin"

  - it: tracing enabled emits enable + endpoint + samplingRate args
    set:
      observability.tracing.enabled: true
      observability.tracing.endpoint: http://otel-collector:4317
      observability.tracing.samplingRate: 0.1
    asserts:
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.serverMetrics.tracing.enabled=true"
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.serverMetrics.tracing.endpoint=http://otel-collector:4317"
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.serverMetrics.tracing.samplingRate=0.1"

  - it: tracing enabled registers the TracingPlugin
    set:
      observability.tracing.enabled: true
    asserts:
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.plugins=Tracing:com.arcadedb.tracing.TracingPlugin"

  - it: otlp + tracing + a custom plugin all appear in one comma-joined plugin list
    set:
      observability.metrics.otlp.enabled: true
      observability.tracing.enabled: true
      arcadedb.plugins.myplugin.enabled: true
      arcadedb.plugins.myplugin.port: 1234
      arcadedb.plugins.myplugin.class: com.example.MyPlugin
    asserts:
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.plugins=myplugin:com.example.MyPlugin,Otlp:com.arcadedb.metrics.otlp.OtlpMetricsPlugin,Tracing:com.arcadedb.tracing.TracingPlugin"

  - it: readinessRequiresHA emits the arg plus the default readinessHAMaxLag
    set:
      observability.health.readinessRequiresHA: true
    asserts:
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.readinessRequiresHA=true"
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.readinessHAMaxLag=100"

  - it: readinessHAMaxLag can be overridden
    set:
      observability.health.readinessRequiresHA: true
      observability.health.readinessHAMaxLag: 25
    asserts:
      - contains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.readinessHAMaxLag=25"

  - it: readinessHAMaxLag is not emitted when readinessRequiresHA is false
    set:
      observability.health.readinessHAMaxLag: 25
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.readinessHAMaxLag=25"

  - it: renders without panic when observability is explicitly null
    set:
      observability: null
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.logFormat=json"
      - equal:
          path: spec.template.spec.containers[0].livenessProbe.httpGet.path
          value: /api/v1/health

  - it: renders without panic when observability sub-maps are explicitly null
    set:
      observability.logging: null
      observability.metrics: null
      observability.tracing: null
      observability.health: null
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.server.readinessRequiresHA=true"
      - notContains:
          path: spec.template.spec.containers[0].command
          content: "-Darcadedb.serverMetrics.otlp.enabled=true"

  - it: scrape pod annotations render when enabled with the prometheus plugin
    set:
      arcadedb.plugins.prometheus.enabled: true
      observability.metrics.prometheus.podAnnotations.enabled: true
    asserts:
      - equal:
          path: spec.template.metadata.annotations["prometheus.io/scrape"]
          value: "true"
      - equal:
          path: spec.template.metadata.annotations["prometheus.io/port"]
          value: "2480"
      - equal:
          path: spec.template.metadata.annotations["prometheus.io/path"]
          value: /prometheus

  - it: scrape pod annotation port honours service.http.port and custom path
    set:
      arcadedb.plugins.prometheus.enabled: true
      service.http.port: 9090
      observability.metrics.prometheus.podAnnotations.enabled: true
      observability.metrics.prometheus.podAnnotations.path: /metrics
    asserts:
      - equal:
          path: spec.template.metadata.annotations["prometheus.io/port"]
          value: "9090"
      - equal:
          path: spec.template.metadata.annotations["prometheus.io/path"]
          value: /metrics

  - it: scrape pod annotation port can be overridden explicitly
    set:
      arcadedb.plugins.prometheus.enabled: true
      observability.metrics.prometheus.podAnnotations.enabled: true
      observability.metrics.prometheus.podAnnotations.port: 1234
    asserts:
      - equal:
          path: spec.template.metadata.annotations["prometheus.io/port"]
          value: "1234"

  - it: user podAnnotations are preserved alongside scrape annotations
    set:
      arcadedb.plugins.prometheus.enabled: true
      podAnnotations:
        my-team: data
      observability.metrics.prometheus.podAnnotations.enabled: true
    asserts:
      - equal:
          path: spec.template.metadata.annotations["my-team"]
          value: data
      - equal:
          path: spec.template.metadata.annotations["prometheus.io/scrape"]
          value: "true"

  - it: no scrape annotations when podAnnotations discovery is disabled
    asserts:
      - notExists:
          path: spec.template.metadata.annotations["prometheus.io/scrape"]

  - it: fails when scrape annotations enabled without the prometheus plugin
    set:
      observability.metrics.prometheus.podAnnotations.enabled: true
    asserts:
      - failedTemplate:
          errorPattern: "require arcadedb.plugins.prometheus.enabled=true"

  - it: fails when serviceMonitor enabled without the prometheus plugin
    set:
      observability.metrics.prometheus.serviceMonitor.enabled: true
    asserts:
      - failedTemplate:
          errorPattern: "require arcadedb.plugins.prometheus.enabled=true"
